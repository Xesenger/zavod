```json
{
  "decision": "REJECT",
  "rationale": "The Worker returned status 'unavailable' with only a summary that restates the task description verbatim, no actual modifications, no blockers, and no warnings. This indicates the Worker did not execute the task—it merely echoed the intent without producing code changes, integration points, or evidence of end-to-end FPS update flow implementation. The acceptance criteria require evidence-backed output for the project; a restatement of the task is not evidence of work completed.",
  "issues": [
    "Worker status is 'unavailable', indicating task was not executed",
    "Summary is a direct copy of the task description, not a result summary",
    "No modifications field populated—no code changes or integration points provided",
    "No evidence of render loop analysis, FPS calculation integration, or HUD update implementation"
  ],
  "next_action": "Escalate or reassign: the Worker did not attempt the task; clarify whether the project root is accessible and retry with explicit anchors (e.g., file paths to render loop entry point, HUD container selector) if the task is to proceed."
}
```