```json
{
  "decision": "REVISE",
  "rationale": "The Worker claims to have added FPS display styling to src/ui/hud.css but the staged execution shows 'SKIPPED insert_after' due to a non-unique anchor, meaning no actual modification was written to the staging sandbox. The Worker's status is 'partial' and explicitly lists a blocker (JavaScript implementation needed), confirming the task is incomplete. While the CSS-only scope is valid, the execution failure combined with the acknowledged missing JavaScript component means the core deliverable—a functional FPS display in the top-right corner—has not been achieved. A clarified brief with a concrete anchor point or a narrower scope (CSS only, with JavaScript deferred) would likely allow recovery.",
  "issues": [
    "Staged execution skipped due to non-unique anchor in src/ui/hud.css",
    "No actual file modifications written to staging sandbox despite claimed edit",
    "Task marked partial with unresolved blocker (JavaScript FPS calculation)",
    "Acceptance criteria require evidence-backed output; staged artefacts are absent"
  ],
  "next_action": "Return to Worker with a revised brief specifying the exact anchor location in hud.css and clarifying whether JavaScript implementation is in scope for this task or deferred to a follow-up."
}
```