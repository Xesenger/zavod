```json
{
  "decision": "REVISE",
  "rationale": "The Worker returned status 'partial' with two active blockers: positioning requirements and anchor uniqueness for CSS insertion. While staged artefacts exist with concrete byte deltas (hud.js 2967B→3017B, hud.css 14771B→14881B), the blockers indicate core work is incomplete. The task requires adding 'ZAVOD OK' display via existing HUD mechanism without layout changes, but the Worker has not resolved the technical anchors needed to finalize the implementation. The modifications are planned but not validated as safe or complete.",
  "issues": [
    "Blocker: exact positioning requirements for custom message not confirmed",
    "Blocker: anchor uniqueness uncertain for CSS insertion point",
    "Status 'partial' indicates incomplete execution relative to acceptance criteria"
  ],
  "next_action": "Return to Worker with clarified positioning constraints (e.g., 'display alongside health/armor in existing HUD row') and request validation of CSS selector uniqueness before re-execution."
}
```