```json
{
  "decision": "REVISE",
  "rationale": "The Worker's status is 'partial' and explicitly declares a blocker: 'Need to integrate FPS updates with the main render loop.' The acceptance criteria require a complete, evidence-backed output for the task. While staged artefacts confirm CSS and JS modifications were written (hud.css and hud.js with concrete byte deltas), the core task—displaying FPS in the top-right corner of the existing HUD—is incomplete without render loop integration. The FPS tracking logic exists but is not wired to the active rendering pipeline, meaning the feature does not yet function end-to-end.",
  "issues": [
    "Task status is 'partial' with an unresolved blocker on render loop integration",
    "FPS display cannot update without connection to the main render loop",
    "Acceptance criteria require complete, functional output; partial implementation does not meet this bar"
  ],
  "next_action": "Return a clarified brief anchoring the Worker to integrate FPS updates into the main render loop (e.g., specify the render function name or loop entry point) and retry as a bounded follow-up task."
}
```